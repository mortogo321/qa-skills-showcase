# All values are optional — the suite defaults to the public demo credentials
# documented by SauceDemo and Restful-Booker. Override only when pointing the
# tests at a different environment.
SAUCE_PASSWORD=
BOOKER_USERNAME=
BOOKER_PASSWORD=
